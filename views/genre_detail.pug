//- ベースレイアウトを継承
extends layout

//- メインコンテンツブロック
block content

  //- ジャンル名表示
  h1 ジャンル: #{genre.name}

  //- 書籍リスト表示エリア
  div(style='margin-left:20px;margin-top:20px')
    //- 書籍リスト見出し
    h2(style='font-size: 1.5rem;') 書籍一覧
    
    //- 書籍が存在する場合
    if genre_books.length
      dl
        each book in genre_books
          dt
            a(href=book.url) #{book.title}  //- 書籍タイトル（リンク付き）
          dd #{book.summary}  //- 書籍の概要
    else
      //- 書籍が存在しない場合
      p このジャンルに属する書籍はありません。

  //- 操作メニュー
  hr
  p
    a(href=genre.url+'/delete') ジャンルを削除
  p
    a(href=genre.url+'/update') ジャンルを更新